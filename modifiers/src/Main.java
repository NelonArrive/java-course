public class Main {
	public static void main(String[] args) {
	
	}
}

// ⚫ public — доступен всем
// Класс, метод или переменная, объявленные как public,
// видны откуда угодно — из любого пакета и любого класса.

// 🔵 private — доступ только внутри того же класса
// Используется для инкапсуляции данных, чтобы их нельзя было менять напрямую.

// 🔴 protected — доступен в том же пакете и в наследниках
// Используется, если нужно дать доступ дочерним классам, даже из других
// пакетов.

// ⚪ default (package-private) — если модификатор не указан
// По умолчанию элемент доступен только внутри того же пакета.
